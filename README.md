# openshift-multi-daemonset-example
This manifest deploys the CrowdStrike Falcon Node Sensor across all nodes in an OpenShift cluster using the Falcon Operator. It creates two separate DaemonSets to ensure complete node coverage across master and worker nodes. 
